<!-- Keep it short. The why matters more than the what. -->

## What and why

Closes #

## How it was verified

<!-- Which suites you ran, or the manual steps. CI runs all of them. -->

- [ ] `npm test` and `npm run typecheck`
- [ ] `npm run e2e` (only if you touched the server, adapters, SDK, or examples)
- [ ] `cd worker && python -m pytest` (only if you touched `worker/`)
- [ ] Docs pair (`docs/md/` + `docs/html/`) updated, if behaviour changed
